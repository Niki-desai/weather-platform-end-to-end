# Docker Environment Variables (for docker-compose)
# This file is used when running services inside Docker containers

# API Gateway
PORT=3000

# Redis (Docker service name)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379

# RabbitMQ (Docker service name)
RABBITMQ_URL=amqp://rabbitmq:5672

# Kafka (Docker service name)
KAFKA_BROKER=kafka:9092

# Database (Docker service name)
POSTGRES_USER=weather
POSTGRES_PASSWORD=weather
POSTGRES_DB=weather_db
DATABASE_URL=postgresql://weather:weather@postgres:5432/weather_db

# External APIs (add your API keys here)
OPENWEATHER_API_KEY=your_api_key_here
